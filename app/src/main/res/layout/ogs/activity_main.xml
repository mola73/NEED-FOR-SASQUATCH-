<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android" android:id="@+id/main"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <!-- Start Menu Background Image -->
    <ImageView android:id="@+id/start_menu_background" android:layout_width="match_parent"
        android:layout_height="match_parent" android:scaleType="centerCrop"
        android:src="@drawable/start_menu" android:contentDescription="Start Menu Background" />

    <!-- Start ImageButton -->
    <ImageButton android:id="@+id/start_button" android:layout_width="150dp"
        android:layout_height="150dp" android:layout_centerInParent="true"
        android:background="@drawable/start_button" android:contentDescription="Start Button"
        android:scaleType="fitCenter" android:src="@drawable/start_button" />
</RelativeLayout>
