package BackendInfo.Mode;

public class RunawayMode extends Mode {
    public RunawayMode() {
        super();
    }

    public RunawayMode(int score, double time) {
        super(score, time);
    }
}
